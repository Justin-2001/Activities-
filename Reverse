#include<stdio.h>

int main()
{
    int num, r, d;
    
    printf("Input number: ");
    scanf("%d", &num);
    
    while(num)
    {
    d=num % 10;
    r= r*10+d;
    num= num/10;
    }
    printf("reverse %d", r);
    return 0;
}
